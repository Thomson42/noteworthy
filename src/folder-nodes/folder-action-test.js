import * as actions from './folder-node-constants';
import { foldersFetchDataSuccess, foldersAreLoading } from './recipe.actions';

describe('folder actions', () => {
    it('loads a file', () => {
        const api = {};
        const folder = {};
        
        let dispatched = [];
        const dispatch = (action) =>{
            dispatched.push(action);
        };

    });
    it('gets all the files and notes', () => {
        
    });
});